# Python
__pycache__/
*.py[cod]

# Image Mode Studio runtime artifacts
.studio.pid
studio.log
studio.log.err
.buildkitd.toml

# Build output (RAW/qcow2 images can be multi-GB)
output/
bootc-output/

# Harvested RPM cache (pulled via your own Red Hat entitlement — not ours to
# redistribute via git)
rpm-cache/

# Hand-dropped RPMs for the manual build (keep the dir, not the packages)
rpms/*.rpm
